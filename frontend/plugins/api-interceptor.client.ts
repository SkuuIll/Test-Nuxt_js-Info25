export default defineNuxtPlugin(() => {
    // This plugin is disabled for now to avoid conflicts
    // The auth headers are handled directly in the composables
    console.log('API interceptor plugin loaded')
})